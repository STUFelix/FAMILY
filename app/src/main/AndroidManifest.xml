<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kaixuan.family">

    <uses-permission android:name="android.permission.INTERNET"></uses-permission>
    <!--以下权限在使用缓存时用到-->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"></uses-permission>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"></uses-permission>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"></uses-permission>

    <uses-permission android:name="android.permission.READ_DATABASE"></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".login.ToSignInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".FamilyTreeActivity"></activity>
        <activity android:name=".TryActivity"></activity>
        <activity android:name=".Contacts.chatInterfaceActivity"></activity>
        <activity android:name=".MeIntentActivity"></activity>
        <activity android:name=".Circle.HisCircleActivity"></activity>
        <activity android:name=".Me.MyLifeActivity"></activity>
        <activity android:name=".login.ToSignUpActivity"></activity>
        <activity android:name=".MainActivity"></activity>>
    </application>

</manifest>